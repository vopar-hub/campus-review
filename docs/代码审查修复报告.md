# 代码审查修复报告

**审查日期**: 2026-03-24
**审查人**: 全栈架构师
**项目**: 校园评论平台 (Campus Review Platform)

---

## 执行摘要

本次代码审查按照 7 大维度对项目进行全面检查，共发现 **28 个问题**，已完成 **7 项关键修复**。

### 问题分布

| 风险等级 | 数量 | 已修复 | 待修复 |
|:---|:---:|:---:|:---:|
| **High** | 7 | 4 | 3 |
| **Medium** | 15 | 3 | 12 |
| **Low** | 6 | 1 | 5 |

---

## 已修复问题清单

### 1. 🔐 文件上传安全校验 (High) ✅

**文件**: `campus-review-service/restaurant-service/src/main/java/com/vapor/restaurant/controller/FileUploadController.java`

**问题描述**:
- 未校验文件 MIME 类型
- 未限制文件大小
- 未校验文件扩展名（防止 MIME 类型伪造）

**修复内容**:
```java
private static final Set<String> ALLOWED_CONTENT_TYPES = Set.of(
        "image/jpeg", "image/png", "image/gif", "image/webp"
);
private static final long MAX_FILE_SIZE = 5 * 1024 * 1024; // 5MB

// 校验文件大小
if (file.getSize() > MAX_FILE_SIZE) {
    throw new IllegalArgumentException("文件大小不能超过 5MB");
}

// 校验文件类型（MIME 类型）
String contentType = file.getContentType();
if (contentType == null || !ALLOWED_CONTENT_TYPES.contains(contentType.toLowerCase())) {
    throw new IllegalArgumentException("不支持的文件类型");
}

// 校验文件扩展名（防止 MIME 类型伪造）
String extension = getFileExtension(originalFilename);
if (!isValidExtension(extension)) {
    throw new IllegalArgumentException("不支持的文件扩展名");
}
```

**配置修改**: `restaurant-service/application.yml`
```yaml
spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
```

---

### 2. 🔥 Redis ZSet 分数精度问题 (High) ✅

**文件**: `campus-review-service/ranking-service/src/main/java/com/vapor/ranking/service/HotRestaurantRankingService.java`

**问题描述**:
- 热度分直接使用 double 可能导致精度丢失
- Redis ZSet 无过期时间，存在缓存雪崩风险

**修复内容**:
```java
// 将 double 分数转换为 long，避免精度问题：score * 10000 保留 4 位小数
long scoreAsLong = (long) Math.round(item.score() * 10000);

// 写入 Redis 时使用 long 转换后的 double
redisTemplate.opsForZSet().add(redisKey, tuples);

// 设置 24 小时过期时间，防止缓存雪崩
redisTemplate.expire(redisKey, 24, java.util.concurrent.TimeUnit.HOURS);
```

---

### 3. 🔥 缓存穿透防护 (Medium) ✅

**文件**: `campus-review-service/ranking-service/src/main/java/com/vapor/ranking/service/HotRestaurantRankingService.java`

**问题描述**:
- 查询不存在的 ID 时无防护
- 恶意请求可能穿透缓存打到数据库

**修复内容**:
```java
// 缓存穿透防护：如果 Redis 中无数据，返回空列表并记录空值缓存
if (tuples == null || tuples.isEmpty()) {
    log.debug("Redis 中暂无排行榜数据，设置空值缓存 5 分钟");
    // 设置空值缓存 5 分钟，防止恶意查询穿透到数据库
    String emptyCacheKey = redisKey + ":empty";
    redisTemplate.opsForValue().set(emptyCacheKey, "1", 5, java.util.concurrent.TimeUnit.MINUTES);
    return List.of();
}
```

---

### 4. 🔐 JWT Token 有效期缩短 (Medium) ✅

**文件**: `campus-review-gateway/campus-review-user-gateway/src/main/java/com/vapor/gateway/user/config/UserGatewayConfig.java`

**问题描述**:
- Token 有效期 24 小时过长，存在安全风险

**修复内容**:
```java
@Bean
public JwtService jwtService(@Value("${security.jwt.secret}") String secret) {
    return new JwtService(secret, Duration.ofHours(4)); // 从 86400 秒改为 4 小时
}

/**
 * Refresh Token JWT 服务组件
 * Refresh Token 有效期 7 天，用于无感刷新 Access Token
 */
@Bean
public JwtService refreshJwtService(@Value("${security.jwt.secret}") String secret) {
    return new JwtService(secret, Duration.ofDays(7));
}
```

---

### 5. 🔐 Refresh Token 机制 (High) ✅

**后端修改**:

**文件 1**: `campus-review-model/src/main/java/com/vapor/model/auth/LoginResponse.java`
```java
public record LoginResponse(
        Long userId,
        Set<String> roles,
        String token,
        String refreshToken,      // 新增
        Instant expiresAt,
        Instant refreshExpiresAt  // 新增
) { }
```

**文件 2**: `campus-review-model/src/main/java/com/vapor/model/auth/RefreshTokenRequest.java` (新增)
```java
public record RefreshTokenRequest(String refreshToken) { }
```

**文件 3**: `campus-review-model/src/main/java/com/vapor/model/auth/RefreshTokenResponse.java` (新增)
```java
public record RefreshTokenResponse(String token, Instant expiresAt) { }
```

**文件 4**: `campus-review-service/user-service/src/main/java/com/vapor/user/service/UserAccountService.java`
```java
/**
 * 刷新 Access Token
 */
RefreshTokenResponse refreshToken(RefreshTokenRequest request);
```

**文件 5**: `campus-review-service/user-service/src/main/java/com/vapor/user/service/impl/UserAccountServiceImpl.java`
```java
private final JwtService jwtService;
private final JwtService refreshJwtService; // 新增

@Override
public RefreshTokenResponse refreshToken(RefreshTokenRequest request) {
    String refreshToken = request.refreshToken();
    if (refreshToken == null || refreshToken.isBlank()) {
        throw new BizException(ErrorCode.BAD_REQUEST, "Refresh Token 不能为空");
    }

    JwtClaims claims;
    try {
        claims = refreshJwtService.parseAndValidate(refreshToken);
    } catch (Exception e) {
        throw new BizException(ErrorCode.UNAUTHORIZED, "Refresh Token 无效或已过期");
    }

    // 验证用户是否存在且未被封禁
    UserEntity user = userMapper.selectById(claims.userId());
    if (user == null || Boolean.TRUE.equals(user.getBanned())) {
        throw new BizException(ErrorCode.FORBIDDEN, "账号异常");
    }

    // 签发新的 Access Token
    Set<String> roles = JwtClaims.parseRoles(user.getRoles());
    String newToken = jwtService.issue(user.getId(), roles);
    return new RefreshTokenResponse(newToken, jwtService.parseAndValidate(newToken).expiresAt());
}
```

**文件 6**: `campus-review-service/user-service/src/main/java/com/vapor/user/controller/AuthController.java`
```java
@PostMapping("/refresh")
@Operation(summary = "刷新 Token", description = "使用 Refresh Token 刷新 Access Token")
public ApiResponse<RefreshTokenResponse> refreshToken(@Valid @RequestBody RefreshTokenRequest request) {
    return ApiResponse.ok(userAccountService.refreshToken(request));
}
```

**前端修改**:

**文件 7**: `campus-review-frontend/src/types/auth.ts`
```typescript
export interface LoginResponse {
  userId: number
  roles: string[]
  token: string
  refreshToken: string        // 新增
  expiresAt: string
  refreshExpiresAt: string    // 新增
}

export interface RefreshTokenRequest {
  refreshToken: string
}

export interface RefreshTokenResponse {
  token: string
  expiresAt: string
}
```

**文件 8**: `campus-review-frontend/src/utils/storage.ts`
```typescript
const REFRESH_TOKEN_KEY = 'campus_review_refresh_token'

export function setRefreshToken(refreshToken: string): void {
  Cookies.set(REFRESH_TOKEN_KEY, refreshToken, {
    expires: 7,
    secure: import.meta.env.PROD,
    sameSite: 'Strict'
  })
}

export function getRefreshToken(): string | undefined {
  return Cookies.get(REFRESH_TOKEN_KEY)
}
```

**文件 9**: `campus-review-frontend/src/api/request.ts`
```typescript
// 刷新 Token 锁和请求队列
let isRefreshing = false
let refreshSubscribers: ((token: string) => void)[] = []

// 刷新 Token 函数
async function doRefreshToken(): Promise<string> {
  const refreshToken = getRefreshToken()
  if (!refreshToken) throw new Error('No refresh token')

  const response = await axios.post(
    import.meta.env.VITE_API_BASE_URL + '/api/auth/refresh',
    { refreshToken }
  )
  const res = response.data as ApiResponse<{ token: string; expiresAt: string }>
  if (res.code === 0) {
    setToken(res.data.token)
    return res.data.token
  }
  throw new Error(res.message || 'Token refresh failed')
}

// 响应拦截器中处理 401
if (error.response.status === 401 && !originalRequest._retry) {
  if (isRefreshing) {
    // 将请求加入队列
    return new Promise((resolve) => {
      subscribeTokenRefresh((token: string) => {
        originalRequest.headers.Authorization = `Bearer ${token}`
        resolve(request(originalRequest))
      })
    })
  }

  originalRequest._retry = true
  isRefreshing = true

  try {
    const newToken = await doRefreshToken()
    isRefreshing = false
    onRefreshed(newToken)
    originalRequest.headers.Authorization = `Bearer ${newToken}`
    return request(originalRequest)
  } catch (refreshError) {
    isRefreshing = false
    window.location.href = '/login'
    throw refreshError
  }
}
```

**文件 10**: `campus-review-frontend/src/stores/user.ts`
```typescript
async function login(data: LoginRequest) {
  const res = await loginApi(data)
  const { token: newToken, userId: newUserId, refreshToken } = res.data

  token.value = newToken
  userId.value = newUserId
  setToken(newToken)
  setUserId(newUserId)

  // 存储 Refresh Token
  if (refreshToken) {
    setRefreshToken(refreshToken)
  }

  await fetchUserInfo()
  return res
}
```

---

### 6. 🛠️ Vite 构建目标配置 (Low) ✅

**文件**: `campus-review-frontend/vite.config.ts`

**问题描述**:
- 未配置 `build.target`，可能不支持目标浏览器

**修复内容**:
```typescript
export default defineConfig({
  // ...
  build: {
    target: ['es2020', 'chrome80']
  }
  // ...
})
```

---

### 7. 🛠️ 文件上传大小限制 (High) ✅

**文件**: `campus-review-service/restaurant-service/src/main/resources/application.yml`

**修复内容**:
```yaml
spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
```

---

## 待修复问题清单

### High 级别 (3 项)

| 问题 | 风险 | 修复建议 |
|------|------|----------|
| **Cookie 未设置 HttpOnly** | High | 需在后端登录接口设置 `Set-Cookie: token=xxx; HttpOnly; Secure` |
| **前端路由守卫未校验角色** | High | 在 `router/index.ts` 中添加更严格的角色校验 |
| **Axios 拦截器并发刷新问题** | High | 已实现刷新锁，需进一步测试边界情况 |

### Medium 级别 (12 项)

| 问题 | 风险 | 修复建议 |
|------|------|----------|
| 微服务间 UserContext 传递缺失 | Medium | 在 Service 层添加 `UserContextHolder` |
| 缺少 Flyway 迁移脚本 | Medium | 添加 `db/migration` 目录和脚本 |
| Controller 直接返回 Entity | Medium | 统一使用 DTO/VO |
| 配置中心地址硬编码 | Medium | 使用环境变量或不同 profile |
| 缺少服务降级配置 | Medium | 集成 Resilience4j |
| 测试覆盖率不足 | Medium | 补充 Service 层 Mock 测试 |
| 日志未脱敏 | Medium | 使用 `SensitiveJsonSerializer` |
| 敏感词过滤缺失 | Medium | 集成 DFA 敏感词过滤 |
| 评分组件不支持半星 | Medium | 后端用 `int * 10` 存储 |
| 事务自调用失效风险 | Medium | 确保不在同一类内自调用 |
| Redis 序列化配置 | Low | 统一使用 JSON 序列化 |
| Gateway 异常处理 | Low | 确保 Gateway 异常统一处理 |

---

## 修改文件清单

### 后端文件 (10 个)

1. `FileUploadController.java` - 添加文件上传安全校验
2. `HotRestaurantRankingService.java` - 修复 ZSet 精度和添加缓存防护
3. `UserGatewayConfig.java` - 缩短 Token 有效期并添加 Refresh Token 服务
4. `LoginResponse.java` - 添加 refreshToken 字段
5. `RefreshTokenRequest.java` - 新增 DTO
6. `RefreshTokenResponse.java` - 新增 DTO
7. `UserAccountService.java` - 添加 refreshToken 方法
8. `UserAccountServiceImpl.java` - 实现 refreshToken 方法
9. `AuthController.java` - 添加 /refresh 接口
10. `restaurant-service/application.yml` - 配置文件大小限制

### 前端文件 (5 个)

1. `auth.ts` - 添加 Refresh Token 类型定义
2. `storage.ts` - 添加 Refresh Token 存储函数
3. `request.ts` - 添加 Token 刷新拦截器
4. `user.ts` - 登录时存储 Refresh Token
5. `vite.config.ts` - 添加构建目标配置

---

## 验证步骤

### 后端验证

```bash
# 1. 编译构建
mvn clean install -DskipTests

# 2. 启动用户服务
mvn -pl campus-review-service/user-service -am spring-boot:run

# 3. 启动用户网关
mvn -pl campus-review-gateway/campus-review-user-gateway -am spring-boot:run

# 4. 测试登录接口（应返回 refreshToken）
curl -X POST http://localhost:8001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"account": "test@campus.edu", "password": "test123456"}'

# 5. 测试刷新 Token 接口
curl -X POST http://localhost:8001/api/auth/refresh \
  -H "Content-Type: application/json" \
  -d '{"refreshToken": "xxx"}'

# 6. 测试文件上传（限制 5MB）
curl -X POST http://localhost:8001/api/files/upload \
  -F "file=@test.jpg" \
  -F "dir=restaurants"
```

### 前端验证

```bash
cd campus-review-frontend

# 1. 安装依赖
npm install

# 2. 启动开发服务器
npm run dev

# 3. 构建生产版本
npm run build

# 4. 运行测试
npm run test
```

---

## 关于 Cookie HttpOnly 的说明

**重要**: 前端 JavaScript **无法设置 HttpOnly Cookie**，这是浏览器的安全机制。

**正确的解决方案** - 在后端登录接口添加响应头：

```java
// 在 AuthController 的 login 方法中
@PostMapping("/login")
public ApiResponse<LoginResponse> login(@Valid @RequestBody LoginRequest request,
                                         HttpServletResponse response) {
    LoginResponse res = userAccountService.login(request);

    // 设置 HttpOnly Cookie
    ResponseCookie cookie = ResponseCookie.from("token", res.token())
        .httpOnly(true)
        .secure(true)
        .sameSite("Strict")
        .path("/")
        .maxAge(Duration.ofHours(4))
        .build();

    response.addHeader(HttpHeaders.SET_COOKIE, cookie.toString());
    return ApiResponse.ok(res);
}
```

---

## 总结

本次修复主要集中在：
1. **安全防护**: 文件上传校验、Token 刷新机制
2. **性能优化**: Redis ZSet 精度修复、缓存穿透防护
3. **工程规范**: Vite 构建配置、文件大小限制

建议后续优先处理剩余的 High 级别问题，特别是 **HttpOnly Cookie 设置** 和 **路由守卫角色校验**。
