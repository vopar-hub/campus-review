# 开发环境配置
# 使用相对路径，由 nginx 反向代理分发到不同的后端服务
# 前端统一请求当前域名（默认 80 端口），nginx 负责路由分发
VITE_API_BASE_URL=

# 如果需要指定端口，可以设置为：
# VITE_API_BASE_URL=http://localhost:80
# 或者通过环境变量动态设置：
# VITE_API_BASE_URL=http://localhost:${VITE_SERVER_PORT:-80}
