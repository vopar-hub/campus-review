server:
  port: 8101

spring:
  application:
    name: user-service
  config:
    import[0]: optional:nacos:user-service.yaml?group=DEFAULT_GROUP
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
        import-check:
          enabled: true




