spring:
  cloud:
    nacos:
      config:
        enabled: false
      discovery:
        enabled: false
    compatibility-verifier:
      enabled: false
  application:
    name: ranking-service

downstream:
  restaurant-service-base-url: http://localhost:8102
  interaction-service-base-url: http://localhost:8104
  review-service-base-url: http://localhost:8103

ranking:
  hot-restaurants:
    refresh-ms: 60000
